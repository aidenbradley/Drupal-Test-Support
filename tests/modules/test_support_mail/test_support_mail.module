<?php

function test_support_mail_mail($key, &$message): void
{
    if ($key !== 'test_support_mail') {
        return;
    }

    $message['subject'] = \Drupal::state()->get('test_support.mail_subject');
    $message['body'][] = \Drupal::state()->get('test_support.mail_body');
}
